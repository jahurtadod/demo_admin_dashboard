import 'package:flutter/material.dart';

class DashboarView extends StatelessWidget {
  const DashboarView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Bashboard View'),
      ),
    );
  }
}
